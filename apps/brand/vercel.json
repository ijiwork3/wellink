{
  "installCommand": "npm install --workspaces --include-workspace-root",
  "buildCommand": "npm run build -w packages/ui && npm run build -w apps/brand",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
