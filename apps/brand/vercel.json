{
  "buildCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --frozen-lockfile=false && pnpm --filter @wellink/ui build && pnpm --filter wellink-brand build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
